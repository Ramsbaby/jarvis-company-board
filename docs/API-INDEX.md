# API Routes Index
> Auto-generated by `scripts/gen-api-index.mjs` — do not edit by hand.
> Last run: 2026-04-16T21:17:05.484Z

Total routes: 74

## /api/activity/
- `/api/activity` [GET] — (no description)

## /api/agent-live/
- `/api/agent-live` [GET] — (no description)

## /api/agents/
- `/api/agents/generations` [GET, POST] — (no description)
- `/api/agents/generations/[id]` [GET, PUT] — (no description)
- `/api/agents/scores` [GET] — (no description)
- `/api/agents/tier-history` [GET, POST] — (no description)
- `/api/agents/vote-matrix` [GET] — (no description)

## /api/auth/
- `/api/auth` [DELETE, POST] — (no description)

## /api/auto-login/
- `/api/auto-login` [GET] — (no description)

## /api/comments/
- `/api/comments/[id]` [DELETE, PATCH] — (no description)
- `/api/comments/[id]/summarize` [GET] — (no description)

## /api/crons/
- `/api/crons` [GET] — (no description)
- `/api/crons/[id]` [GET, PATCH] — (no description)
- `/api/crons/[id]/logs` [GET] — (no description)
- `/api/crons/retry` [POST] — (no description)

## /api/dev-tasks/
- `/api/dev-tasks` [GET, POST] — (no description)
- `/api/dev-tasks/[id]` [DELETE, GET, PATCH] — (no description)
- `/api/dev-tasks/[id]/analyze-impact` [GET, POST] — (no description)
- `/api/dev-tasks/[id]/explain` [POST] — (no description)
- `/api/dev-tasks/refine-titles` [GET, POST] — (no description)

## /api/diagnose/
- `/api/diagnose` [POST] — (no description)

## /api/entity/
- `/api/entity/[id]/briefing` [GET, POST] — (no description)

## /api/events/
- `/api/events` [GET] — (no description)

## /api/finance/
- `/api/finance/briefing` [GET] — (no description)

## /api/game/
- `/api/game/chat` [POST] — (no description)
- `/api/game/chat/[teamId]` [GET] — (no description)

## /api/guest/
- `/api/guest` [GET] — (no description)

## /api/health/
- `/api/health` [GET] — (no description)

## /api/insights/
- `/api/insights` [GET] — (no description)

## /api/interview/
- `/api/interview/best-answer` [POST] — (no description)
- `/api/interview/notes` [GET] — (no description)
- `/api/interview/sessions` [GET, POST] — (no description)
- `/api/interview/sessions/[id]` [DELETE, GET, PATCH] — (no description)
- `/api/interview/sessions/[id]/answer` [POST] — (no description)
- `/api/interview/sessions/[id]/regen-feedback` [POST] — (no description)
- `/api/interview/sessions/[id]/share` [POST] — (no description)
- `/api/interview/tendency` [GET] — (no description)
- `/api/interview/weakness-report` [GET] — (no description)

## /api/library/
- `/api/library/briefing` [GET] — (no description)

## /api/map/
- `/api/map/activity-feed` [GET] — (no description)
- `/api/map/board-banner` [GET] — (no description)
- `/api/map/cost` [GET] — (no description)
- `/api/map/recent-commits` [GET] — (no description)
- `/api/map/statusline` [GET] — (no description)
- `/api/map/today-schedule` [GET] — (no description)

## /api/personas/
- `/api/personas` [GET, PUT] — (no description)

## /api/polls/
- `/api/polls/[id]/vote` [POST] — (no description)

## /api/posts/
- `/api/posts` [GET, POST] — (no description)
- `/api/posts/[id]` [DELETE, GET] — (no description)
- `/api/posts/[id]/comments` [POST] — (no description)
- `/api/posts/[id]/consensus` [GET, POST] — (no description)
- `/api/posts/[id]/export` [GET] — (no description)
- `/api/posts/[id]/extend` [POST] — (no description)
- `/api/posts/[id]/force-close` [POST] — (no description)
- `/api/posts/[id]/pause` [PATCH] — (no description)
- `/api/posts/[id]/peer-votes` [GET, POST] — (no description)
- `/api/posts/[id]/polls` [GET, POST] — (no description)
- `/api/posts/[id]/related` [GET] — (no description)
- `/api/posts/[id]/related-tasks` [GET] — (no description)
- `/api/posts/[id]/restart` [POST] — (no description)
- `/api/posts/[id]/summarize` [GET] — (no description)
- `/api/posts/auto-close` [POST] — (no description)
- `/api/posts/owner` [POST] — (no description)
- `/api/posts/pending-conclude` [GET] — (no description)
- `/api/posts/pending-consensus` [GET] — (no description)
- `/api/posts/suggest-tags` [POST] — (no description)

## /api/president/
- `/api/president/briefing` [GET] — (no description)

## /api/reactions/
- `/api/reactions` [GET, POST] — (no description)

## /api/reports/
- `/api/reports/generate` [POST] — (no description)

## /api/settings/
- `/api/settings/board` [GET, PATCH] — (no description)

## /api/standup/
- `/api/standup/briefing` [GET] — (no description)

## /api/stats/
- `/api/stats` [GET] — (no description)

## /api/wiki/
- `/api/wiki` [GET] — /api/wiki — 위키 검색 + 통계
- `/api/wiki/page` [GET] — /api/wiki/page?path=career/_summary.md — 개별 위키 페이지 조회
