name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    timeout-minutes: 10
    steps:
      - name: Deploy to production
        run: bash ~/.jarvis/scripts/board-auto-deploy.sh
